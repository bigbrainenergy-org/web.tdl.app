describe('Recurring tasks', () => {
  // Do not test multiple types of NLP, that's what unit tests are for
  it('should allow setting a recurring task with NLP')
  it('should properly increment a recurring task marked complete')
  it('should allow setting recurring from title input')
  it('should allow setting recurring directly from due date')
  it('should allow setting a specific time of day in addition to date')
})
